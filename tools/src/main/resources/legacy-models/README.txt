The files in this folder have the suffix -version.dmr, where version matches the EAP release these were created from.

The exception is -wf14.dmr, which is what will become EAP 7.2.0. Once that is out all files with the -wf14.dmr suffix should
be renamed to -7.2.0.dmr.